{
  "name": "clervio-app",
  "version": "1.0.0",
  "private": true,
  "description": "CLERVIO — centre de pilotage privé pour achats, garanties et abonnements",
  "scripts": {
    "check": "node scripts/validate.mjs",
    "test": "node scripts/validate.mjs && node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
